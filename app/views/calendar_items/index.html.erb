<%  @page_title = "予定表" %>
<h1><%=  @page_title %></h1>

<p><%= link_to "予定の新規登録", :new_calendar_item  %></p>
<p>
  <% CalendarItem::CATEGORY_NAMES.each do |k,v| %>
    <%= link_to v, category_calendar_items_path(category: k) %>
      <!-- #以下のやり方も出来るが望ましくない -->
     <!-- <%= link_to v, "categories/#{k}/calendar_items" %> -->
<% end %>
</p>

<%= render "list" %>
